{
  "name": "scl-workspace",
  "private": true,
  "description": "The workspace for developing scl.js.",
  "devDependencies": {
    "@babel/core": "^7.5.5",
    "@babel/preset-env": "^7.5.5",
    "@types/chai": "^4.2.1",
    "@types/mocha": "^5.2.7",
    "@types/node": "^12.7.4",
    "benchmark": "^2.1.4",
    "chai": "^4.2.0",
    "coveralls": "^3.0.6",
    "del": "^5.1.0",
    "fs-extra": "^8.1.0",
    "gulp": "^4.0.2",
    "gulp-babel": "^8.0.0",
    "gulp-mocha": "^7.0.1",
    "gulp-rename": "^1.4.0",
    "gulp-sourcemaps": "^2.6.5",
    "gulp-typescript": "^5.0.1",
    "istanbul": "^0.4.5",
    "merge-char": "^1.3.4",
    "merge2": "^1.2.4",
    "mocha": "^6.2.0",
    "nyc": "^14.1.1",
    "source-map-support": "^0.5.13",
    "ts-node": "^8.3.0",
    "typescript": "^3.6.2",
    "xxhashjs": "^0.2.2"
  },
  "scripts": {
    "coveralls": "nyc report --reporter=text-lcov | coveralls",
    "build": "gulp",
    "test": "nyc mocha 'src/test/**/*.ts' --reporter min --require ts-node/register --require source-map-support/register",
    "build-docs": "typedoc src/ --exclude src/test/ --out docs --listInvalidSymbolLinks --includeDeclarations --excludeExternals --excludeNotExported --excludeExternals --excludeProtected --mode file --name 'Standard Collections Library for JavaScript' --readme readme-docs.md"
  },
  "authors": [
    "Sam Vervaeck <vervaeck.sam@skynet.be>"
  ],
  "license": "MIT",
  "dependencies": {
    "@types/benchmark": "^1.0.31"
  }
}
